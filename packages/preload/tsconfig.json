{
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    "baseUrl": "."
  },

  "include": [
    "src/**/*.ts",
    "contracts.d.ts",
    "../../types/**/*.d.ts",
    "../../types/**/*.ts"
  ],
  "exclude": ["**/*.spec.ts", "**/*.test.ts", "*.config.js", "dist"]
}
