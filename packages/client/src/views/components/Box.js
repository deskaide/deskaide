import styled from 'styled-components';
import {
  space,
  layout,
  position,
  color,
  typography,
  flexbox,
} from 'styled-system';

const Box = styled.div`
  ${space};
  ${layout};
  ${position};
  ${color};
  ${typography};
  ${flexbox};
`;

export default Box;
