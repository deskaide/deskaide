{
  "name": "deskaide",
  "private": true,
  "scripts": {
    "up": "lerna bootstrap",
    "start": "lerna exec --parallel --stream npm start"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
